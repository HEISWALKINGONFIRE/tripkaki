<h1>Welcome back, <%= @user.first_name + " " + @user.last_name %></h1><br/>

<h2>Your Public Reservations</h2><br/>

<h2>Your Private Reservations</h2><br/>
<p>
	<% @user.private_reservations.each do |private_reservation| %>
		<li><%= link_to private_reservation.package.title, package_private_reservation_path(private_reservation.package, private_reservation) %></li>

	<% end %>
</p>